# FoldFX downloads — SHA-256 checksums
# Verify before installing: sha256sum <file> (Linux/macOS) or certutil -hashfile <file> SHA256 (Windows)
# This build is byte-identical to the FoldFX-0.2.6-signed.apk asset on the FoldFX GitHub release.
# Note: the release signing key was rotated AGAIN for 0.2.6 — if you have 0.2.5 or older
# installed, uninstall it before updating (Android refuses in-place updates across signing
# identities). License keys are unaffected and re-activate as normal.

6d42a3c4f09f4b5ffb73bd2b463a3dbf557113c07cdff1a41cb4a82bde279698  FoldFX-0.2.6-release.apk  (0.2.6 / versionCode 8, release-signed — the same build buyers receive)
